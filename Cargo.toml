[package]
name = "demsos"
version = "0.0.0"
edition = "2021"
description = "Desmos command-line image uploader."
documentation = "https://docs.rs/demsos"
readme = "README.md"
repository = "https://github.com/7ap/demsos"
license = "MIT"

[dependencies]
clap = { version = "4.0.15", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
